const groupReducer = (state = {
    result:1,
    lastValues: []
},action) => {
    switch(action.type){
        case "SEARCH_GROUPS":
        break;
    }
    return state;
};

export default groupReducer;